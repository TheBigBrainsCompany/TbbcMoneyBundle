tbbc_money:
    currencies: ["USD", "EUR"]
    reference_currency: "EUR"

framework:
    trusted_hosts: ~
    secret: "test"
    test: ~

doctrine:
    dbal:
        driver:   'pdo_sqlite'
        memory:   true
    orm:
        entity_managers:
            default: ~
